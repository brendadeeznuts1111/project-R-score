{
  "_id": "@factorywager/registry-client",
  "name": "@factorywager/registry-client",
  "description": "Runtime-neutral HTTP SDK for the FactoryWager artifact registry",
  "dist-tags": {
    "latest": "1.0.0"
  },
  "versions": {
    "1.0.0": {
      "name": "@factorywager/registry-client",
      "version": "1.0.0",
      "description": "Runtime-neutral HTTP SDK for the FactoryWager artifact registry",
      "type": "module",
      "main": "./dist/index.js",
      "module": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "bun": "./src/index.ts",
          "types": "./dist/index.d.ts",
          "import": "./dist/index.js",
          "default": "./src/index.ts"
        },
        "./package.json": "./package.json"
      },
      "files": [
        "dist",
        "README.md"
      ],
      "sideEffects": false,
      "dependencies": {},
      "peerDependencies": {
        "bun-types": ">=1.3.0"
      },
      "peerDependenciesMeta": {
        "bun-types": {
          "optional": true
        }
      },
      "publishConfig": {
        "access": "public"
      },
      "scripts": {
        "build": "bun build ./src/index.ts --outdir ./dist --target browser --format esm && bunx tsc --project ./tsconfig.json",
        "test": "bun test ../../tests/registry-sdk.test.ts",
        "prepublishOnly": "bun run build"
      },
      "_id": "@factorywager/registry-client@1.0.0",
      "dist": {
        "tarball": "https://registry.factory-wager.com/api/registry/npm/@factorywager/registry-client/-/registry-client-1.0.0.tgz",
        "integrity": ""
      },
      "readme": "# @factorywager/registry-client\n\nHTTP client for the FactoryWager registry. It uses Web APIs only and runs in\nBun, browsers, and Cloudflare Workers.\n\n```ts\nimport { RegistryClient } from '@factorywager/registry-client';\n\nconst client = new RegistryClient({\n  baseUrl: 'https://registry.factory-wager.com',\n  publishUrl: 'https://registry-write.internal.factory-wager.com',\n  apiKey: runtimeConfig.factoryWagerToken,\n});\n\nconst artifact = await client.resolve('@factorywager/routing-algorithms');\nconst bytes = await client.download('@factorywager/routing-algorithms');\n```\n\n`download()` verifies both the indexed byte length and SHA-256 checksum.\nPublishing targets the authenticated private registry API; the Cloudflare\nPages/R2 endpoint remains read-only. The API key is sent only to `publishUrl`;\nanonymous health, index, and artifact requests never receive it.\n",
      "readmeFilename": "README.md"
    }
  },
  "time": {
    "created": "2026-08-02T22:42:47.496Z",
    "modified": "2026-08-02T22:42:47.496Z",
    "1.0.0": "2026-08-02T22:42:47.496Z"
  },
  "readme": "# @factorywager/registry-client\n\nHTTP client for the FactoryWager registry. It uses Web APIs only and runs in\nBun, browsers, and Cloudflare Workers.\n\n```ts\nimport { RegistryClient } from '@factorywager/registry-client';\n\nconst client = new RegistryClient({\n  baseUrl: 'https://registry.factory-wager.com',\n  publishUrl: 'https://registry-write.internal.factory-wager.com',\n  apiKey: runtimeConfig.factoryWagerToken,\n});\n\nconst artifact = await client.resolve('@factorywager/routing-algorithms');\nconst bytes = await client.download('@factorywager/routing-algorithms');\n```\n\n`download()` verifies both the indexed byte length and SHA-256 checksum.\nPublishing targets the authenticated private registry API; the Cloudflare\nPages/R2 endpoint remains read-only. The API key is sent only to `publishUrl`;\nanonymous health, index, and artifact requests never receive it.\n",
  "readmeFilename": "README.md"
}
