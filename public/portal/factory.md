# Factory Registry

Tenant operations portal for the Factory Registry bot.

- Bot: `factorywager_bot`
- Webhook: `/api/telegram/webhook/factory`
- Registry: `/registry/factory/registry.json`

See `/portal/factory/` for the live dashboard.
