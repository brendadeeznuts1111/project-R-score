# Bun Configuration for Performance Optimization
# 
# These environment variables configure Bun's HTTP request limits
# https://bun.com/docs/runtime/networking/fetch#:~:text=BUN_CONFIG_MAX_HTTP_REQUESTS=512

# Maximum simultaneous HTTP requests (default: 512)
BUN_CONFIG_MAX_HTTP_REQUESTS=512

# Maximum simultaneous HTTP requests per host (default: 6)
BUN_CONFIG_MAX_HTTP_REQUESTS_PER_HOST=6

# Additional performance optimizations
BUN_FEATURE_FLAG_DISABLE_NATIVE_DEPENDENCY_LINKER=0
BUN_FEATURE_FLAG_DISABLE_IGNORE_SCRIPTS=0

# Memory and performance tuning
NODE_OPTIONS="--max-old-space-size=1024"
