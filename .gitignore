# FactoryWager Enterprise Platform - .gitignore

# Machine layout — separate clone symlinked under dev root (see ~/.config/shell/machine-paths.sh)
/kimi-toolchain

# Vendored upstream clones (local only)
/plannotator-upstream/

# Local symlinks inside nested enterprise repos
projects/active/enterprise/fantasy42-fire22-registry/enterprise/packages/dashboard-worker

# Dependencies
node_modules/
bun.lockb
bun.lock
package-lock.json
pnpm-lock.yaml
yarn.lock
.bun-cache/
.bun/install/cache/
.pnpm-store/
.yarn/
.yarn/cache/

# Absolute path caches (from embedded projects)
**/$HOME/
*/.bun/install/cache/
**/bun/install/cache/

# Build outputs
dist/
dist-staging/
.artifacts/
build/
!src/build/
out/
.next/
.nuxt/
.vite/
.output/
.turbo/
.cache/
.cheatsheet-cache/
*.js
# Shebang fixture must be .js for #!/usr/bin/env node (runtime-cli-boundaries)
!tests/fixtures/runtime-cli/shebang-bun/cli.js
# Cloudflare Pages portal modules (tracked as source, not build output)
!public/portal/**/*.js
# Local `factory create` scaffold dumps (nested .git) — never root clutter
/factory-library/
*.js.map
*.tsbuildinfo

# IDE — root workspace config only; nested .vscode stays local
.vscode/
!/.vscode/
!/.vscode/**
**/.vscode/*.md
.idea/
*.swp
*.swo

# Cursor — root MCP config only
.cursor/
!/.cursor/
!/.cursor/mcp.json

# Cursor auto-generated boilerplate & session artifacts
**/.cursor/rules/use-bun-instead-of-node-vite-npm-pnpm.mdc
**/.cursor/plans/
**/.cursor/.agent-tools/
**/.cursor/agents/

# Other IDE / agent tooling
.claude/*
!.claude/agents/
!.claude/commands/
.windsurf/
.codex/
.agents/*
!.agents/skills/
!.agents/skills/**/SKILL.md
!.agents/skills/**/agents/
!.agents/skills/**/agents/**
!.agents/skills/**/scripts/
!.agents/skills/**/scripts/**
!.agents/skills/**/package.json
!.agents/skills/**/SOURCE
!.agents/skills/**/.gitignore
!.agents/skills/**/references/
!.agents/skills/**/references/**
!.agents/skills/**/rules/
!.agents/skills/**/rules/**
!.agents/skills/**/outline-rules/
!.agents/skills/**/outline-rules/**
!.agents/skills/**/mcp/
!.agents/skills/**/mcp/**
!.agents/skills/**/repo-map.json
!.agents/skills/**/sgconfig.yml
!.agents/skills/**/codemods.json
!.agents/skills/**/scan-profiles.json
!.agents/skills/**/tests/
!.agents/skills/**/tests/**
.reasonix/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
*.jsonl
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bun reference profile (may contain enterprise secrets — not auto-loaded)
.bunfig.toml

# Environment variables
.env
.env.local
.env.secret.local
.env.*.local
.env.*
!/.env.example
.env.development
.env.production
.env.staging
.env.test
.env.secret
.env.enc

# Test coverage
coverage/
.nyc_output/
test-results/

# Temporary files
tmp/
temp/
.tmp/

# Project-specific
profiles/
*.profile.json
*-profile-*.json
*.cpuprofile
CPU.*.md
Heap.*.md

# Large data and cache directories
cache/
logs/
performance-results/
reports/
.archive/
*.archive/

# Working directories
data/
output/
download/
# Root frozen dump only — do not match projects/archive/ (tracked tier bucket)
/archive/
# Nested historical dumps inside active projects (keep local; tier is projects/archive/)
projects/active/**/archive/

# Generated and temporary content
*.scratch.*
*.temp.*
*.cache.*
*.output.*
*.download.*

# Temporary and example files
example*-*
test*-*
!scripts/test-*.ts
*.tmp

# Generated files
*.generated.*
*.auto.*
*.build.*
*.dist.*

# Build artifacts and large binaries
*.rdb
*.bun-build
*.tar.gz
*.tgz
*.tar
*.zip

# Development artifacts
*.local.*
*.dev.*
*.debug.*

# Native addons
*.node
*.so
*.dylib

# Session data
sessions/
cookies/

# Deployment artifacts
deploy/
.deploy/

# Uploads
uploads/
public/uploads/

# Projects without tokens or sensitive configurations
*config/
*secrets/
*.token
*.key
*.pem
*.p12
*.pfx
auth/
credentials/
.tokens/
.secrets/
.sensitive/

# Project directories that might contain sensitive data
**/config/
**/secrets/
**/auth/
!functions/api/auth/
!functions/api/auth/**
!functions-bun-only/api/auth/
!functions-bun-only/api/auth/**
**/credentials/
!config/
config/*
!config/domain-branding.json
# Homebase harness SSOT (must clone into worktrees; not machine secrets)
!config/bun-dx-catalog.ts
!config/eslint/
!config/eslint/**

# Runtime artifacts
profiles.db
deployment.log
dump.rdb
metrics/
plugins/
wrangler.toml

# Clawdbot - temporarily ignored
clawdbot/

# NPM Cache (should not be committed)
.npm-cache/

# Separate projects with their own git history
projects/active/kimiremote/
/projects/active/enterprise/bet-ticker-worker-v1.1/
/projects/active/enterprise/cascade-mover-v3/
projects/active/f402-openapi/

# Runtime / ephemeral state (cascade-mover shadow beta)
projects/active/enterprise/cascade-mover-v3/shadow-beta-state.json

# Accidental scaffold paths
projects/active/utilities/shortcut-registry/eeeeee/
projects/active/development/geelark/dev/.vscode/.vscode/

# Secrets and credentials (should never be committed)
bun.secrets
.fw-config.json
*.local.json

# SQLite artifacts (development databases)
*.db-shm
*.db-wal

# Runtime databases (should never be committed)
*.db
*.sqlite
*.sqlite3

# Heap snapshots and memory dumps
*.heapsnapshot
Heap-*
*.heapsnapshot

# Compiled binaries and object files
*.bin
*.o
*.obj
*.exe

# ML models (large binary files)
**/train-anomaly*
**/models/*.pkl
**/models/*.h5

# Binary assets
*.icns

# Smart environment cache (generated AI artifacts)
.smart-env/

# Docs archives (large snapshots)
docs/archives/
!docs/archives/root-cleanup-history.md

# Minified assets
*.min.js
*.min.css
*.bundle.js

# Vendored / third-party
vendor/
.third_party/

# Independent projects — own git history (under projects/)
projects/experimental/apex-biolabs/
projects/experimental/alchemy-effect/
projects/experimental/cloudflare-artifacts/
projects/experimental/purelytelegram-router/
projects/archive/lazycodex/

# Local workspace clutter — worktrees, session artifacts
.kimi/
herdr-worktrees/
/~/
/index.html
/index.ts
photo_*.jpg
artifacts/*.docx
artifacts/*.zip

# Harness / pre-commit regenerable (local only — never stage)
# Docs claim these are gitignored: scripts/BUN_NATIVE.md · docs/harness/FEEDBACK.md
artifacts/bun-native-discover*.json
artifacts/reports/
# reports/ already ignored above (harness-gate-timing · ci-*-timing · inventories)

# Scratch: default-deny local dumps; curated Bun playground only
/scratch/**
!/scratch/README.md
!/scratch/bun-v1.3.9-examples/
!/scratch/bun-v1.3.9-examples/**
# Local bench output under the allowlisted tree
/scratch/bun-v1.3.9-examples/**/reports/

# Standalone nested git repos (own remotes) — not monorepo spine
# (plannotator-upstream/ already listed under "Vendored upstream clones")
/Proton-workspace/
/bet-turnin-sheet/
/bradley-terry/
/toc-ops/
/toc-ops-repo/
/toc-ops-repo-wt-brand-ship/
/toc-ops-worktrees/
examples/demos/alchemy-tutorial/
examples/mega-liner-v7/
examples/mega-liner-v8/

# Compiled single-file executables (bun build --compile)
tools/bin/bun-doc-refs
# Bun --compile compare dumps (no extension; pairs from size/perf checks)
test-binary-*
**/sports-terminal-before
**/sports-terminal-after
# Root-level accidental CLI names (nested product binaries live under ignored trees)
/cascade-mover
/mega-liner
# Bun compile scratch + common drop-in binaries
**/.*.bun-build
*.bun-build
**/bin/compiled/
**/hyperbun-core
**/test-embedded
**/train-anomaly
**/*.dmg

# Agent-context bloat (regenerable / local only; reports/ already ignored above)
docs/*.zip
.agents/skills/**/node_modules/

# Bun heap profiles (bun --heap-prof-md) — never commit
lib/profile.md
**/*heap-prof*.md
**/*.heapprofile
