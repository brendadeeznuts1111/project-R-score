# 👥 **USERS DASHBOARD** *(nolarose)*

**Last Update**: Loading... | **0 Users**

---

## 🔄 **Controls**

```dataviewjs
dv.button("🔄 Refresh Users", async () => {
  const { execSync } = require('child_process');
  try {
    execSync('bun users:list nolarose');
    dv.view.reload();
  } catch (error) {
    console.error('Failed to refresh:', error.message);
  }
});

dv.button("📊 Agent Stats", async () => {
  const { execSync } = require('child_process');
  try {
    execSync('bun datapipe:stats');
  } catch (error) {
    console.error('Failed to get stats:', error.message);
  }
});
```

---

## 📊 **USERS TABLE**

```dataview
TABLE
  username AS "Username",
  balance AS "Balance",
  status AS "Status",
  created AS "Created"
FROM "dashboards/users-nolarose"
SORT balance DESC
LIMIT 50
```

---

## 💰 **HIGH BALANCE USERS** *(>$1000)*

```dataview
TABLE
  username AS "Username",
  balance AS "Balance"
FROM "dashboards/users-nolarose"
WHERE balance > 1000
SORT balance DESC
LIMIT 20
```

---

## 📈 **SUMMARY STATS**

```dataviewjs
// Calculate summary stats
const users = dv.pages('"dashboards/users-nolarose"').flatMap(p => p.players || []);

if (users.length > 0) {
  const totalBalance = users.reduce((sum, user) => sum + (parseFloat(user.balance) || 0), 0);
  const avgBalance = totalBalance / users.length;
  const highBalanceUsers = users.filter(user => (parseFloat(user.balance) || 0) > 1000).length;

  dv.el("div", `
    <div style="background: #f5f5f5; padding: 15px; border-radius: 5px; margin: 10px 0;">
      <h4>📊 User Statistics</h4>
      <ul style="margin: 0; padding-left: 20px;">
        <li><strong>Total Users:</strong> ${users.length}</li>
        <li><strong>Total Balance:</strong> $${totalBalance.toLocaleString()}</li>
        <li><strong>Average Balance:</strong> $${avgBalance.toFixed(2)}</li>
        <li><strong>High Balance Users (> $1000):</strong> ${highBalanceUsers}</li>
      </ul>
    </div>
  `);
} else {
  dv.el("div", `
    <div style="background: #fff3e0; padding: 15px; border-radius: 5px; margin: 10px 0;">
      <p><strong>No user data available.</strong> Run <code>bun users:list</code> to fetch users.</p>
    </div>
  `);
}
```

---

*Generated by Datapipe v2.14 • User management • Session-aware*
