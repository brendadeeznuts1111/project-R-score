/**
 * Default ESLint discovery → harness policy (Bun-native + wire boundary).
 * Full-project archival policy: config/eslint/legacy/full-project.config.ts
 */
export { default } from './eslint.harness.config.ts';
