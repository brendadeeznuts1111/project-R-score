{
  "name": "utils",
  "version": "1.0.0",
  "scripts": {
    "build": "echo '[utils] Building utilities...' && sleep 0.5 && echo '[utils] Build complete'",
    "test": "echo '[utils] Running tests...' && sleep 0.5 && echo '[utils] Tests passed'"
  }
}
