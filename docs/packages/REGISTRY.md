# Package Registry Manifest

Generated table — refresh on demand (not day-loop SSOT).

```bash
bun run packages:list
bun run packages:list --write   # rewrite this file
```

See [README.md](./README.md).
