{
	"name": "@bun-toml/benchmark",
	"version": "1.0.0",
	"description": "Benchmark suite for bun-toml-secrets-editor performance testing",
	"license": "MIT",
	"author": "bun-toml-secrets-editor",
	"repository": {
		"type": "git",
		"url": "https://github.com/bun-toml-secrets-editor/bun-toml-secrets-editor.git",
		"directory": "packages/benchmark"
	},
	"engines": {
		"bun": ">=1.3.7"
	},
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"bin": {
		"bun-toml-bench": "./dist/cli.js"
	},
	"exports": {
		".": {
			"import": "./src/index.ts",
			"types": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "bun build ./src/index.ts --outdir ./dist --target bun --minify --splitting --sourcemap=external",
		"test": "bun test",
		"bench": "bun run src/cli.ts",
		"typecheck": "bunx tsc --noEmit",
		"clean": "rm -rf dist",
		"prepublishOnly": "bun run clean && bun run build && bun test"
	},
	"dependencies": {
		"@bun-toml/core": "workspace:*",
		"@bun-toml/rss": "workspace:*",
		"@bun-toml/shared": "workspace:*"
	},
	"devDependencies": {
		"bun-types": "latest",
		"typescript": "^5.9.3"
	},
	"files": [
		"dist/",
		"src/",
		"README.md",
		"LICENSE"
	],
	"keywords": [
		"bun",
		"toml",
		"benchmark",
		"performance",
		"testing"
	]
}
