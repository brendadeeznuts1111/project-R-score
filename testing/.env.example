# ☁️ Cloudflare R2 Configuration
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# R2 Account ID (found in Cloudflare Dashboard > R2 > Overview)
R2_ACCOUNT_ID=your-account-id-here

# R2 API Token (create at: Cloudflare Dashboard > R2 > Manage R2 API Tokens)
# Token needs: R2 Read, R2 Write permissions
R2_ACCESS_KEY_ID=your-access-key-id

# R2 Secret Key (shown once when creating API token)
R2_SECRET_ACCESS_KEY=your-secret-access-key

# R2 Bucket Name (create in Cloudflare Dashboard > R2)
R2_BUCKET_NAME=dev-hq-assets

# Optional: Environment
# ENVIRONMENT=development
