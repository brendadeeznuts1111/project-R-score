{
  "name": "edge-worker",
  "version": "1.0.0",
  "description": "Edge function deployer with Bun",
  "main": "worker.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "bun worker.ts --local",
    "typecheck": "bun cli-resolver.ts typecheck"
  },
  "dependencies": {
    "wrangler": "^3.0.0",
    "itty-router": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "bun",
    "edge",
    "worker",
    "deploy"
  ],
  "author": "ashley",
  "license": "MIT",
  "private": true,
  "engines": {
    "bun": ">=1.3.8"
  },
  "publishConfig": {
    "registry": "https://registry.factory-wager.com"
  }
}
