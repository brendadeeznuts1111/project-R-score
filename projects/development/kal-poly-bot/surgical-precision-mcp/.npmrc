# Surgical Precision MCP - NPM Registry Configuration
# Bun v1.3.5+ Environment Variable Expansion in .npmrc
# Reference: https://bun.com/blog/bun-v1.3.5#environment-variable-expansion-in-npmrc-quoted-values

# GitHub Package Registry for @brendadeeznuts1111 scope
@brendadeeznuts1111:registry=https://npm.pkg.github.com

# Authentication token using Bun v1.3.5 environment variable expansion
# The ? modifier gracefully handles undefined vars (expands to empty string)
# Supports all three syntaxes: ${VAR}, "${VAR}", '${VAR}'
//npm.pkg.github.com/:_authToken=${GITHUB_NPM_TOKEN?}

# Optional: Always authenticate for GitHub Package Registry
# //npm.pkg.github.com/:always-auth=true
