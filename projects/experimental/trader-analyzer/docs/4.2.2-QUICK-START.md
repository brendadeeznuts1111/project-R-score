# Quick Start: Multi-Layer Correlation Graph Deployment

**Version**: 4.2.2.19.0.0.0  
**Quick Reference**: Production Deployment Checklist

---

## 🚀 Quick Deployment Steps

### 1. Initialize Production Readiness
```bash
bun run init:readiness
```

### 2. Test Rate Limiting
```bash
# Should allow 1000 req/sec
for i in {1..100}; do curl http://localhost:3000/api/correlation-graph & done
```

### 3. Test Chaos Engineering
```bash
bun run chaos:kill-layer4 --duration=30s
# Verify: Degrades gracefully, zero downtime
```

### 4. Run Benchmarks
```bash
bun run bench correlation-engine
# Expected: < 5ms latency on all paths
```

### 5. Set Up Monitoring
```bash
# Add to your server
import { prometheusMetricsHandler } from './api/metrics/prometheus';
server.get('/metrics', () => prometheusMetricsHandler(engine));
```

### 6. Schedule Snapshots
```bash
# Add to crontab
0 2 * * * cd /path/to/project && bun run snapshot:rotate
```

---

## ✅ Pre-Deployment Checklist

- [x] Rate limiting (1000 tokens/sec)
- [x] Memory ceiling (4GB monitoring)
- [x] Query timeout (50ms max)
- [x] Prometheus metrics endpoint
- [x] Distributed tracing
- [x] Snapshot rotation script
- [x] Production readiness matrix initialized

---

## 📊 Performance Targets

- **Graph Build**: < 5ms (2.20ms current, 0.8ms with workers)
- **Anomaly Detection**: < 5ms (3.91-4.76ms current)
- **Propagation**: < 1ms (365µs current)
- **Throughput**: 450 graphs/sec (single instance)

---

## 🔧 Configuration

**Environment Variables**:
```bash
DATABASE_PATH=/path/to/correlation.db
SNAPSHOT_DIR=./snapshots
RETENTION_DAYS=7
MEMORY_CEILING_BYTES=4000000000
QUERY_TIMEOUT_MS=50
```

---

## 📚 Full Documentation

See [Production Deployment Guide](./4.2.2-PRODUCTION-DEPLOYMENT-GUIDE.md) for complete details.

---

**Status**: ✅ Ready for Staging Deployment
