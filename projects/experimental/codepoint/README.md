# codepoint

Nested proxy / dashboard sandbox (experimental). Child apps (`bun-proxy/`, `web-project/`, dashboards, …) stay inside this root.

| | |
|--|--|
| Tier | `experimental` · [triage](../../README.md) |
| Install | `bun install` (from this root or a child app) |

```bash
cd projects/experimental/codepoint
bun install
```
