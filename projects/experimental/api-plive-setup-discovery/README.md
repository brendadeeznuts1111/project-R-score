# api-plive-setup-discovery

One-shot Plive API discovery / workflow wrapper (experimental).

| | |
|--|--|
| Tier | `experimental` · [triage](../../README.md) |
| Install | `bun install` |

```bash
cd projects/experimental/api-plive-setup-discovery
bun install
```
