# Expanded analytics ownership

This package path preserves analytics-team ownership for the expanded dashboard.
The canonical runtime and documentation tree is
[`../../../../analytics/`](../../../../analytics/).

Do not copy the dashboard payload into this package. Routes, manifests, and
registry uploads resolve project-root `analytics/` paths.
