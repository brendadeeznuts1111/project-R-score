# Bun configuration for Empire Pro Config Manager
# See https://bun.sh/docs/runtime/bunfig

[build]
root = "src"

[test]
root = "tests"
testTimeout = 5000

[dev]
hot = false

[install]
exact = true


[install.scopes]
"@factorywager" = { url = "https://registry.factory-wager.com/api/npm" }
