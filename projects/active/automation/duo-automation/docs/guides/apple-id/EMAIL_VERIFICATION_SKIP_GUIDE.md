# 🍎 Apple ID Creation - Email Verification Skip Guide

## 🎯 Overview

Complete guide for creating Apple ID accounts without email verification, including batch creation, configuration management, and automation options.

## ✅ What's Available

### **🚀 Quick Start Commands**

```bash
# Create 3 accounts without email verification
bun run create-no-verify

# Create custom batch without verification
bun run create-appleid.js --skip-verification --country=US --count=5

# Disable email verification globally
bun run disable-email

# Re-enable email verification
bun run enable-email
```

### **📱 Account Creation Options**

- **Single Account**: Create one account at a time
- **Batch Creation**: Create multiple accounts in sequence
- **Skip Verification**: Bypass email verification step
- **Custom Configuration**: Country, device, proxy options

---

## 🛠️ Configuration Files

### **Email Configuration** (`config/email.js`)

```javascript
export default {
  "verification": {
    "enabled": false,        // Set to false to skip verification
    "autoVerify": true,
    "timeout": 30000,
    "retryAttempts": 3
  },
  "smtp": {
    "enabled": false,        // Set to false to disable SMTP
    "host": "smtp.gmail.com",
    "port": 587,
    "secure": false,
    "auth": {
      "user": "utahj4754@gmail.com",
      "pass": "ecaq beoe ccio lekd"
    }
  },
  "imap": {
    "enabled": false,        // Set to false to disable IMAP
    "host": "imap.gmail.com",
    "port": 993,
    "secure": true,
    "auth": {
      "user": "utahj4754@gmail.com",
      "pass": "ecaq beoe ccio lekd"
    }
  }
};
```

---

## 🚀 Available Commands

### **Quick Commands**

```bash
# Create 3 accounts without verification (quickest)
bun run create-no-verify

# Create single account with verification
bun run create-appleid.js --count=1

# Create 5 accounts without verification, custom password
bun run create-appleid.js --skip-verification --count=5 --password=MyPass123!

# Create accounts for specific country
bun run create-appleid.js --skip-verification --country=UK --count=3
```

### **Configuration Management**

```bash
# Disable email verification globally
bun run disable-email

# Re-enable email verification
bun run enable-email

# Check current email configuration
cat config/email.js
```

### **Advanced Options**

```bash
# Create with specific device
bun run create-appleid.js --skip-verification --device=emulator-5554 --count=2

# Create with proxy
bun run create-appleid.js --skip-verification --proxy=residential --count=3

# Create with custom city
bun run create-appleid.js --skip-verification --city="New York" --count=2

# Verbose output
bun run create-appleid.js --skip-verification --verbose --count=1
```

---

## 📊 Batch Creation Features

### **Automatic Rate Limiting**

- **Delay Between Accounts**: 5-10 seconds random delay
- **Error Handling**: Automatic retry on failures
- **Progress Tracking**: Real-time progress updates

### **Result Management**

- **JSON Export**: Detailed account information
- **CSV Export**: Easy spreadsheet import
- **Timestamped Files**: Organized by creation time
- **Success Rate Tracking**: Performance metrics

### **Example Batch Output**

```text
🚀 Starting batch creation of 3 accounts
🌍 Country: US
📧 Email Verification: SKIPPED
============================================================

📊 Progress: 1/3 accounts
🍎 Creating Account 1/3
──────────────────────────────────────────────────
✅ Account created successfully!
📧 Email: john.doe2024@factory-wager.com
🔑 Password: AutoGenerated123!
🍎 Apple ID: john.doe2024@factory-wager.com
⚠️  Email verification SKIPPED - Manual verification required

⏳ Waiting 7s before next account...

📊 Progress: 2/3 accounts
...
```

---

## 📁 File Structure

### **Created Files**

```text
accounts/
├── batch-3-2026-01-12T13-30-00-000Z.json    # Detailed JSON report
├── batch-3-2026-01-12T13-30-00-000Z.csv     # CSV for spreadsheets
└── ...
```

### **Configuration Files**

```text
config/
├── email.js                                    # Email verification settings
└── config.json                                 # Main configuration
```

---

## 🌍 Country Options

### **Supported Countries**

- **US**: United States (default)
- **UK**: United Kingdom
- **CA**: Canada
- **AU**: Australia
- **DE**: Germany

### **Country-Specific Features**

- **Demographic Matching**: Name/email patterns by country
- **Proxy Selection**: Country-specific proxy options
- **Device Settings**: Regional device configurations

---

## 🔄 Email Verification Toggle

### **Method 1: Command Line Flag**

```bash
# Skip verification for one command
bun run create-appleid.js --skip-verification --count=3
```

### **Method 2: Global Configuration**

```bash
# Disable globally
bun run disable-email

# All subsequent commands will skip verification
bun run create-appleid.js --count=5

# Re-enable when needed
bun run enable-email
```

### **Method 3: Manual Edit**

```bash
# Edit config file directly
sed -i '' 's/"enabled": true/"enabled": false/g' config/email.js
```

---

## 📱 Device Management

### **Automatic Device Detection**

```bash
# Automatically finds connected devices
bun run create-appleid.js --skip-verification --count=2
```

### **Manual Device Selection**

```bash
# Use specific device
bun run create-appleid.js --skip-verification --device=emulator-5554 --count=1
```

### **Available Devices**

- **Physical Devices**: Connected Android/iOS devices
- **Emulators**: Android emulators
- **Simulation Mode**: When no devices found

---

## 🔧 Advanced Configuration

### **Custom Passwords**

```bash
# Use specific password for all accounts
bun run create-appleid.js --skip-verification --password=MySecurePass123! --count=3
```

### **Proxy Configuration**

```bash
# Use residential proxy
bun run create-appleid.js --skip-verification --proxy=residential --count=2

# Use VPN
bun run create-appleid.js --skip-verification --proxy=vpn --count=2

# Use public proxy
bun run create-appleid.js --skip-verification --proxy=public --count=2
```

### **City Targeting**

```bash
# Target specific city for demographic matching
bun run create-appleid.js --skip-verification --city="New York" --count=2
bun run create-appleid.js --skip-verification --city="London" --count=2
```

---

## 📊 Result Analysis

### **JSON Report Format**

```json
{
  "timestamp": "2026-01-12T13:30:00.000Z",
  "batch": {
    "count": 3,
    "country": "US",
    "skipVerification": true,
    "successful": 3,
    "failed": 0
  },
  "accounts": [
    {
      "email": "john.doe2024@factory-wager.com",
      "password": "AutoGenerated123!",
      "appleId": "john.doe2024@factory-wager.com",
      "country": "US",
      "createdAt": "2026-01-12T13:30:00.000Z"
    }
  ]
}
```

### **CSV Export Format**

```csv
Email,Password,Apple ID,Country,Created At
"john.doe2024@factory-wager.com","AutoGenerated123!","john.doe2024@factory-wager.com","US","2026-01-12T13:30:00.000Z"
```

---

## 🚨 Important Notes

### **Verification Skip Implications**

- ⚠️ **Manual Verification Required**: Accounts must be verified manually
- ⚠️ **Limited Functionality**: Some features may require verification
- ⚠️ **Security Considerations**: Skip verification for testing only

### **Best Practices**

- 🔄 **Small Batches**: Create 3-5 accounts at a time
- ⏱️ **Rate Limiting**: Built-in delays prevent detection
- 📊 **Monitor Results**: Check success rates and adjust
- 💾 **Backup Data**: Save account information securely

### **Troubleshooting**

```bash
# Check email configuration
cat config/email.js

# Test with single account first
bun run create-appleid.js --skip-verification --count=1 --verbose

# Re-enable verification if needed
bun run enable-email
```

---

## 🎯 Quick Start Examples

### **Example 1: Quick Batch (No Verification)**

```bash
bun run create-no-verify
```

### **Example 2: Custom Batch**

```bash
bun run create-appleid.js --skip-verification --country=UK --count=5 --password=UKPass123!
```

### **Example 3: Disable Email Globally**

```bash
bun run disable-email
bun run create-appleid.js --count=10
bun run enable-email  # Re-enable later
```

### **Example 4: Advanced Configuration**

```bash
bun run create-appleid.js \
  --skip-verification \
  --country=US \
  --city="New York" \
  --proxy=residential \
  --password=NYPass123! \
  --count=3 \
  --verbose
```

---

## 🎉 Summary

Your Apple ID creation system now supports:

- ✅ **Email Verification Skip**: Bypass verification step
- ✅ **Batch Creation**: Create multiple accounts efficiently
- ✅ **Configuration Management**: Easy enable/disable verification
- ✅ **Result Export**: JSON and CSV output formats
- ✅ **Rate Limiting**: Built-in protection against detection
- ✅ **Country Support**: Multiple country options
- ✅ **Device Management**: Automatic and manual device selection

**Create accounts like a pro!** 🍎🚀
