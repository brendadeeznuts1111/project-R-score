# Bun Configuration for Surgical Precision Platform
# Generated on: 2026-01-13T22:25:52.260Z

[install]
exact = true
# Cache configuration
cache = true
cache-dir = "./.bun-cache"
# Public packages resolve from npm. Private FactoryWager packages use the
# authenticated scopes below.
registry = "https://registry.npmjs.org/"
# Lockfile configuration
lockfile = true
lockfile-save = true

[run]
# Script execution configuration
shell = "system"
preload = ["./scripts/preload.ts"]

[build]
# Build configuration
target = "bun"
minify = false
sourcemap = true
outdir = "./data/build-artifacts"

[test]
# Test configuration
preload = ["./__tests__/setup.ts"]
coverage = true
coverageDir = "./coverage"

[debug]
# Debug configuration
port = 9229
host = "localhost"

# Environment-specific settings

[dev]
hotReload = true
liveReload = true




[publish]
registry = "https://registry.factory-wager.com"

[install.scopes]
"@factorywager" = { token = "$FW_REGISTRY_TOKEN", url = "https://registry.factory-wager.com/" }
"@duoplus" = { token = "$FW_REGISTRY_TOKEN", url = "https://registry.factory-wager.com/" }
