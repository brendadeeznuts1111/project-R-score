# @factorywager/rip

Ripgrep-backed analysis helpers for this monorepo.

```bash
bun packages/rip/src/cli.ts help
# or (if present on disk)
bun lib/rip/cli.ts help
```

Import: `@factorywager/rip` / workspace package entry `src/index.ts`.

Hub: [../../../README.md](../../../README.md) · Bun APIs: `bun tools/bun-doc-refs.ts suggest "Bun.spawn"`.
