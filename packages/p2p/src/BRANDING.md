# P2P branding notes

Domain copy / alias conventions for the `@factorywager/p2p` library.

Not a CLI package — there is no `test:continuity` or `start:p2p-proxy:*` root script. Use library imports + `bun test packages/p2p` when coverage exists.

See [README.md](./README.md).
