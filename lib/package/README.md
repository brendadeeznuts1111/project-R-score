# package

Package manager graph helpers.

Inventory: [`../README.md`](../README.md). Do not treat nested dumps as new API surface.

| Entry |
|-------|
| [`package-manager.ts`](./package-manager.ts) |

