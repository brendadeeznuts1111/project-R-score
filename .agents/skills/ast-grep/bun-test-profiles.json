{
  "version": 1,
  "min_bun": "1.3.13",
  "description": "bun test CI flag recipes — maps to bun-releases.json 1.3.13 test_cli",
  "profiles": {
    "local": {
      "description": "Per-file isolation without multiprocess overhead",
      "args": ["--isolate"]
    },
    "ci": {
      "description": "Full CI — parallel workers with per-file isolation",
      "args": ["--parallel", "--isolate"]
    },
    "fast": {
      "description": "Fixed worker count for constrained runners",
      "args": ["--parallel=4", "--isolate"]
    },
    "ci-shard": {
      "description": "Matrix shard — set BUN_TEST_SHARD=M/N or pass --shard",
      "args": ["--parallel", "--isolate"],
      "shard": true
    },
    "changed": {
      "description": "Pre-push — only tests affected by git changes",
      "args": ["--changed"]
    },
    "changed-main": {
      "description": "Tests changed since main branch",
      "args": ["--changed=main"]
    },
    "watch-changed": {
      "description": "Watch mode with changed-file filter",
      "args": ["--changed", "--watch"]
    }
  }
}