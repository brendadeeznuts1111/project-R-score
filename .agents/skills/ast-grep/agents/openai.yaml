interface:
  display_name: "ast-grep"
  short_description: "AST map/search/scan for monorepo — map, outline, files, replace"
  default_prompt: "Start with nav/map --heatmap for unfamiliar zones; index for cross-target symbol lookup. Use outline before large reads, rules for inventory, audit for violation counts, fix for autofix rules. Run test after editing YAML rules. See references/monorepo.md."