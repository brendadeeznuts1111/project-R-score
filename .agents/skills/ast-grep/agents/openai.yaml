interface:
  display_name: "ast-grep"
  short_description: "Syntax-aware search, migration audits, and rewrites"
  default_prompt: "Outline unfamiliar code, validate the syntax pattern, use versioned fail-closed audits when available, preview rewrites, and prove changes with the owning project tests."
