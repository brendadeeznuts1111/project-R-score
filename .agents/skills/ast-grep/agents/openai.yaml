interface:
  display_name: "ast-grep"
  short_description: "AST map/search/scan for monorepo — map, outline, files, replace"
  default_prompt: "Start with ast_grep_helper.py map for unfamiliar zones. Use outline before large reads, files for cheap path lists, scan for bundled rules. See references/monorepo.md for workspace paths."