{
  "name": "@projects/ast-grep-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Pinned ast-grep CLI for agent outline/search workflows (0.44.0+ for outline).",
  "scripts": {
    "doctor": "python3 scripts/ast_grep_helper.py doctor",
    "test": "python3 scripts/ast_grep_helper.py test",
    "audit": "python3 scripts/ast_grep_helper.py audit",
    "baseline": "./scripts/baseline.sh",
    "install-cli": "npm install",
    "smoke": "./scripts/smoke.sh"
  },
  "dependencies": {
    "@ast-grep/cli": "0.44.0"
  }
}