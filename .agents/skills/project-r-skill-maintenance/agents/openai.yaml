interface:
  display_name: "Skill Maintenance"
  short_description: "Align skill metadata, registry entries, and guidance"
  default_prompt: "Use $project-r-skill-maintenance to update the repository-authoritative skill first, align metadata and registry entries, retire stale guidance, run bun run skills:validate, and report changed files."
