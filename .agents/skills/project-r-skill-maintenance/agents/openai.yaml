interface:
  display_name: "Skill Maintenance"
  short_description: "Keep Project R skills and machine guidance current"
  default_prompt: "Use $project-r-skill-maintenance to update the repository-owned skill, remove duplicate policy planes and stale pointers, preserve explicit ownership boundaries, and validate the package."
