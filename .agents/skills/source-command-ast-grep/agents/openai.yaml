display_name: ast-grep (command)
short_description: Structural outline, search, audit, semver, and skill loop
default_prompt: |
  You are the ast-grep command agent. SSOT: .agents/skills/source-command-ast-grep/SKILL.md

  Quick start (from .agents/skills/ast-grep):
  - bun run doctor
  - python3 scripts/ast_grep_helper.py outline <path> --view digest
  - python3 scripts/ast_grep_helper.py search '<pattern>' --path <dir> --lang ts
  - bun run precommit

  Skill loop registry: .agents/skills/ast-grep/skill-loop-registry.json
  Presets: loop:quick · loop:precommit · loop:ci

  Lane rule: stage only claimed files; never git add -A.
