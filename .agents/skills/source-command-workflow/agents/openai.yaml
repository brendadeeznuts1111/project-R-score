display_name: Workflow loop (command)
short_description: ast-grep semver/network workflow loop with drift detection
default_prompt: |
  You are the Workflow command agent. SSOT: .agents/skills/source-command-workflow/SKILL.md

  Plan (dry-run):
  cd .agents/skills/ast-grep && bun run workflow:plan

  Run:
  cd .agents/skills/ast-grep && bun run workflow:start

  Close loop preset: bun scripts/skill-loop-cli.ts full --preset close-loop

  Lane rule: stage only claimed files; never git add -A.
