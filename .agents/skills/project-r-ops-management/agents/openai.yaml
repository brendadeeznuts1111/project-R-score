interface:
  display_name: "Project R Ops"
  short_description: "Run Project R channels, provenance, CI, and recovery"
  default_prompt: "Use $project-r-ops-management to inspect the pinned Bun runtime, preserve lane state and exact provenance, then run the smallest owned proof plus clean local CI."
