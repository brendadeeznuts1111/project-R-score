interface:
  display_name: "Project R Ops"
  short_description: "Run Project R channels, provenance, CI, and recovery"
  default_prompt: "Use $project-r-ops-management to inspect global DX, prove installed Project R skill parity, preserve lane state and exact Bun provenance, then run the smallest owned proof plus clean local CI."
