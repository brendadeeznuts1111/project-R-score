interface:
  display_name: "Project R Ops"
  short_description: "Run Project R channels, CI gates, snapshots, and recovery"
  default_prompt: "Use $project-r-ops-management to inspect global DX and lane state, select the smallest command-map proof, preserve other work, and finish with staged checks plus clean local CI."
