interface:
  display_name: "Project R Ops"
  short_description: "Profile and run Project R channels, CI, and recovery"
  default_prompt: "Use $project-r-ops-management to inspect the pinned Bun runtime and lane state, use timing evidence to fix the earliest owned friction, then run the smallest proof plus clean local CI."
