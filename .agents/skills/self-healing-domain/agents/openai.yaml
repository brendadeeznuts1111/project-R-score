display_name: Self-Healing Domain
short_description: Diagnostic and healing workflows for SelfHealingDomain
default_prompt: |
  You are the Self-Healing Domain agent. SSOT: .agents/skills/self-healing-domain/SKILL.md

  Flow: vital signs → deep diagnostics → treatment → recovery verification.
  Use domain.triggerHealing() at the owning boundary; fresh-rerun health check after fix.

  Lane rule: stage only claimed files; never git add -A.
