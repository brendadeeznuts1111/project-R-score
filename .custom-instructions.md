# Development Standards

> **JIT:** Unresolved NFR → one owner in [`docs/harness/README.md`](docs/harness/README.md). Day loop: `bun run harness:status`. Do not load this whole file unless coding-standards detail is the task.


Authoritative **coding conventions** for the FactoryWager monorepo (`~/Projects`).

Aligned with [harness engineering](https://github.com/lopopolo/harness-engineering) (Ryan Lopopolo): the repository teaches the agent—domain types, boundaries, proof, and durable contracts—not tribal memory. Prefer **artifact** over **codebase** when naming what agents maintain and deliver (see [Terminology](#terminology-artifact-not-codebase)).

| Concern | Canonical doc |
|---------|----------------|
| Agent ops / remotes / brands / Bun install | [`AGENTS.md`](AGENTS.md) → [`docs/AGENTS.md`](docs/AGENTS.md) |
| Machine + workspace Bun install policy | [`docs/UNIFIED.md`](docs/UNIFIED.md) |
| Workspace map | [`STRUCTURE.md`](STRUCTURE.md) |
| Human hub + scripts | [`README.md`](README.md) |
| Quick reference (this doc, compressed) | [`docs/DEVELOPMENT-STANDARDS.md`](docs/DEVELOPMENT-STANDARDS.md) |
| Docs index | [`docs/README.md`](docs/README.md) |
| Import boundaries (package graph) | [`docs/IMPORT_BOUNDARIES.md`](docs/IMPORT_BOUNDARIES.md) |
| Wire boundary (parse once) | [`docs/WIRE_BOUNDARY.md`](docs/WIRE_BOUNDARY.md) |
| Path map in code | [`lib/docs/repo-docs.ts`](lib/docs/repo-docs.ts) (`CANONICAL_REPO_DOCS`) |
| External thesis (harness engineering) | [lopopolo/harness-engineering](https://github.com/lopopolo/harness-engineering) |

Do not invent alternate “root structure” or package scripts here — prefer `package.json` and the table above. When you add a root doc, update this table **and** `CANONICAL_REPO_DOCS`.

---

## Terminology: artifact, not codebase

Per [harness durable-systems guidance](https://github.com/lopopolo/harness-engineering/blob/trunk/docs/durable-systems/README.md) ([replace “codebase” with “artifact”](https://x.com/_lopopolo/status/2076878736507736390)):

| Prefer | Avoid | When |
|--------|--------|------|
| **artifact** | codebase | The thing delivered, validated, maintained, or proven (source tree, package, release, report, dataset, doc set) |
| **repository** / **source tree** | codebase | The git tree or files as a location |
| **harness** | “the framework around agents” (vague) | Checks, brands, docs, tools, pre-commit gates that shape agent work |
| **proof** / **evidence** | “it looks fine” | What establishes a claim at the user or ops boundary |

Source is one **replaceable realization** of durable contracts (specs, brands, boundaries, tests). Specs, brands, and ratchets outlive any single implementation pass. See also [Stop Treating Code as the Artifact](https://hyperbo.la/w/code-is-not-the-artifact/).

In docs and agent prose: write “across the artifact”, “migrate the artifact”, “prove the artifact”—not “across the codebase”.

---

## Project

- **Name:** FactoryWager Enterprise Platform
- **Runtime:** Bun (see root `packageManager` / local `bun --version`)
- **Language:** TypeScript (ES modules, `"type": "module"`)
- **Shape:** Monorepo-lite — `packages/*`, `lib/*`, selected `projects/active/*` workspaces
- **Package manager:** Bun only (isolated linker + global store; policy in `docs/UNIFIED.md`)

---

## Domain strings (harness rule) — mandatory for agents

**Domain types replace bare `string`.** Identifiers, credentials, session tokens, account keys, zone IDs, and other domain-valued text must not travel as unbranded `string` inside the harness after the boundary.

**Enforcement:** pre-commit runs `bun tools/branded-id-check.ts --staged --strict` with **no baseline**. New properties **and** mid-line function parameters (`function f(sessionId: string)`) fail the commit. Bare `id: string` / `_id: string` DTO primary keys must be explicitly suppressed with `// brand-ok` — the detector no longer silently auto-suppresses them. Use brands from [`lib/types/branded.ts`](lib/types/branded.ts) or justify with `// brand-ok`.

This follows harness [domain modeling](https://github.com/lopopolo/harness-engineering/blob/trunk/docs/domain-modeling/README.md): [parse, don’t validate](https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/); [branded identifiers](https://x.com/_lopopolo/status/2076191023500611843); [boundary-only decoding](https://x.com/_lopopolo/status/2076200856681357613). Local pattern: [hyperbo.la build](https://github.com/lopopolo/harness-engineering/blob/trunk/docs/domain-modeling/hyperbola.md) (`HyperUrl` instead of raw URL strings between packages).

| Layer | Rule |
|-------|------|
| **Wire / CLI / env / JSON** | Accept `unknown` or `string`; **parse once** with `parseXId` / schema |
| **Optional config** | `tryXId` — never forge empty brands |
| **Known-good internal** | `asXId` when the value is already trusted |
| **Internal APIs** | Parameter types are brands (`SessionId`, `AccountId`, …), not `string` |
| **Display / logs** | May stringify at the edge; do not re-parse mid-stack |

```bash
bun tools/brand-catalog.ts          # discover domain|BrandName
bun run check:brands                # unbranded ID surface (manifest-driven)
bun run check:brands:types          # SessionId ≠ UserId (tsc)
bun run check:brands:all
```

Stable import: [`lib/types/branded.ts`](lib/types/branded.ts). Map: [`lib/types/branded/README.md`](lib/types/branded/README.md). Skill: [`.agents/skills/branded-ids/`](.agents/skills/branded-ids/).

**Bare `string` is fine for:** local loop variables, pure text formatting, non-domain literals, and third-party API shapes **until** they enter our domain—then parse at that boundary.

**Do not:**

- Use `string` for domain IDs in public/harness function signatures
- Write `'' as AccountId` or other empty forges
- Re-validate the same domain invariant in every caller (own it once at parse)

---

## Enforced conventions

ESLint + pre-commit harness enforce these. Remediation CLIs live under `scripts/fix-*.ts`.

| Rule | Do | Don’t |
|------|----|--------|
| Logging | `console.info` / `warn` / `error` | `console.log` |
| Types | `unknown` + guards, real interfaces, **brands for domain strings** | `any`, `as any`, bare `string` for domain IDs |
| Exports | Named exports | `export default` |
| Nullability | Optional chaining / defaults | Non-null assertions (`!`) |
| Errors | Handle or rethrow | Empty `catch` blocks |
| Secrets | Env via `config/ports.ts`, `config/r2-env.ts` | Hardcoded tokens/keys |

```bash
bun run fix:console-log
bun run fix:scan-any-types
bun run fix:scan-default-exports
bun run fix:scan-non-null-assertions
```

### TypeScript habits

- Prefer interfaces for object shapes; **branded IDs** for domain identifiers.
- Explicit return types on public / exported functions when the type is non-obvious.
- Prefer `async`/`await` over long `.then()` chains.
- Result-style unions (`{ ok: true; value } | { ok: false; error }`) are fine for fallible ops.
- **One concept, one owner** — extend the existing brand/module; do not invent a second parser for the same domain value.

### Wire boundary (decode / `unknown` params)

**Full SSOT:** [`docs/WIRE_BOUNDARY.md`](docs/WIRE_BOUNDARY.md) (path/name allowlists, interior vs edge, suppressions).

Harness-engineering: **parse once at the edge**, then pass domain types inward. Code: `BOUNDARY_POLICY` in [`config/eslint/plugin-harness/boundary.ts`](config/eslint/plugin-harness/boundary.ts).

| Rule | ESLint | Meaning |
|------|--------|---------|
| No `decodeUnknownSync` / `decodeUnknown*` outside boundary | `harness/no-decode-unknown-outside-boundary` (**error**) | Only boundary paths or `parse*` / `decode*` / `is*` owners |
| No `unknown` as a function parameter outside boundary | `harness/no-unknown-function-param` (**error** on `HARNESS_PATHS`) | Prefer brands / concrete types |

```ts
// ❌ mid-stack
function handle(docs: unknown) { … }
decodeUnknownSync(schema)(raw)

// ✅ edge
function parseDocs(raw: unknown): Docs { … }          // name marks boundary
function handle(docs: Docs) { … }                     // trusted inward
```

Thesis: [domain-modeling](https://github.com/lopopolo/harness-engineering/blob/trunk/docs/domain-modeling/README.md) · [parse, don’t validate](https://lexi-lambda.github.io/blog/2019/11/05/parse-don-t-validate/).

### Bun APIs

Before using an unfamiliar `Bun.*` API, resolve the canonical doc — do not guess signatures:

```bash
bun tools/bun-doc-refs.ts suggest "Bun.secrets"
bun tools/bun-doc-refs.ts url "Bun.stringWidth"
```

Add `// @see <canonical-url>` near the usage (or `bun tools/bun-doc-refs.ts annotate --write <files>`). Pre-commit blocks staged Bun API use without refs. Details: [`AGENTS.md`](AGENTS.md).

### Console / inspect depth

Use the [`lib/console-depth.ts`](lib/console-depth.ts) helpers for structured output — `inspect` / `logDepth` / `logTable` (human), `inspectTable` (string), `jsonOut` (`--json` machine branches, `{compact: true}` for single-line), `stripANSI` / `colorize` — not raw deep `console.log(obj)`, `console.table`, or `console.log(JSON.stringify(x, null, 2))`. Depth SSOT: `bunfig.toml [console] depth = 6` (wrapper chain: explicit > `--console-depth=N` flag > `BUN_CONSOLE_DEPTH` env > bunfig > 2). Pre-commit `console-format` gate blocks new raw `console.table` / pretty-JSON dumps / direct `Bun.inspect.table` / `console.dir` (`// console-ok` to suppress). Tests: [`tests/console-depth.test.ts`](tests/console-depth.test.ts).

### Harness map (lib)

| Concern | Path |
|---------|------|
| Path SSOT | [`lib/docs/repo-docs.ts`](lib/docs/repo-docs.ts) |
| Brands facade | [`lib/types/branded.ts`](lib/types/branded.ts) |
| Console depth | [`lib/console-depth.ts`](lib/console-depth.ts) |
| Project inventory | [`lib/projects-scan.ts`](lib/projects-scan.ts) |
| R2 credentials | [`lib/security/r2-credentials.ts`](lib/security/r2-credentials.ts) |
| Standards (quick) | [`docs/DEVELOPMENT-STANDARDS.md`](docs/DEVELOPMENT-STANDARDS.md) |
| Lib overview | [`lib/README.md`](lib/README.md) · [`lib/index.ts`](lib/index.ts) (`LIB_INFO`, `FW.standards`) |

---

## Layout (high level)

Full tree: [`STRUCTURE.md`](STRUCTURE.md). Spine only:

```text
Projects/
├── AGENTS.md · README.md · STRUCTURE.md · .custom-instructions.md
├── config/          # ports, r2-env, registry, bunfig pieces
├── lib/             # shared harness (brands, security, scan, console-depth)
├── packages/        # @factorywager/* internal packages
├── projects/active/ # apps (factorywager, sports-terminal-os, …)
├── scripts/         # CI, hygiene, fix-* remediations
├── tools/           # CLIs (bun-doc-refs, brands, overseer, …)
├── docs/            # guides + organization history
├── examples/        # demos (opt-in)
├── server/ services/ src/ tests/ utils/ workers/
└── package.json · bunfig.toml · tsconfig*.json
```

Do not resurrect deleted root parks (`data/`, root `factorywager/`, root `kimiremote/`). Nested products under `projects/active/enterprise/` may be separate remotes (gitignored).

---

## Commands that matter

Prefer named root scripts over ad-hoc paths:

```bash
bun run install:all          # workspace install (isolated + global store)
bun run install:verify       # cache / links / no ./~ drift
bun run validate:workspaces  # workspace coverage
bun run packages:list        # inventory (+ triage)
bun run lint                 # eslint on lib/
bun run lint:harness         # harness eslint config
bun run lint:bun-native:changed  # Bun-native gate (PR/default)
bun run lint:bun-native:rollout  # full harness tree
bun run format:harness       # prettier harness format (`format:core` is an alias)
bun run check:brands:all     # branded ID gates
bun run dev                  # platform server
bun run dashboard            # live dashboards
bun run deployment:readiness
bun run build:affected
bun run test:affected
```

Full list: root `package.json` `scripts`.

---

## Testing & proof

- Runner: **Bun test** (`bun test`, `*.test.ts` / `*.spec.ts`).
- Root harness examples: `tests/` (e.g. `tests/console-depth.test.ts`, `tests/branded-types.test-d.ts`).
- Prefer Arrange → Act → Assert; cover edge cases on public surfaces.
- Project-local tests live next to each app under `projects/active/`.
- **Proof matches the claim** ([harness proof thesis](https://github.com/lopopolo/harness-engineering/blob/trunk/docs/proof/README.md)): unit/type gates prove internal properties; user/ops claims need evidence at that boundary. Prefer promoting the **same artifact** CI validated—do not rebuild under a more privileged job without a new proof chain.

---

## Security & config

- Ports / hosts: `config/ports.ts` (env-overridable).
- R2 / Cloudflare: `config/r2-env.ts` (`CLOUDFLARE_DEFAULTS`) · `.env.example` · `bun run cloudflare:env` · `:assert` / `:assert-apex` / `:assert-live`. Secrets: `CLOUDFLARE_API_TOKEN`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`. Pages dashboard pins: `BUN_VERSION=1.3.14`, `SKIP_DEPENDENCY_INSTALL=true` (identity in defaults, not env). Pack/release R2 URLs: `r2BucketUrlFromEnv()`.
- CORS: `CORS_ALLOWED_ORIGINS` (comma-separated; empty = allow all).
- Bind: `SERVER_HOST` (default `localhost`).
- No `eval` / `Function` constructor on untrusted input; validate wire data before branding (`parse*`).

---

## Delivery

- Conventional commits; pre-commit: repo hygiene, harness lint, branded IDs, Bun doc-refs.
- Parallel agent lanes: claim disjoint paths; don’t stage another session’s dirty files.
- Default: verified work → commit + push to `origin` ([project-R-score](https://github.com/brendadeeznuts1111/project-R-score)). Do not default-push to the `cascade` remote.
- Finish migrations in coherent ratchets when practical—leave one canonical pattern as prompt material, not two eras side by side.

---

## VS Code (recommended)

```json
{
  "recommendations": [
    "oven.bun-vscode",
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint",
    "redhat.vscode-yaml",
    "github.vscode-pull-request-github"
  ]
}
```

---

## Support map

1. Conventions in this file or [`docs/DEVELOPMENT-STANDARDS.md`](docs/DEVELOPMENT-STANDARDS.md)
2. Agent / install / brands → [`AGENTS.md`](AGENTS.md)
3. Layout → [`STRUCTURE.md`](STRUCTURE.md)
4. Harness engineering (external) → [lopopolo/harness-engineering](https://github.com/lopopolo/harness-engineering)
5. Examples → [`examples/`](examples/)
6. Organization history → [`docs/organization/`](docs/organization/)

*Locked-in standards for contributors and automation. Prefer updating this file and [`docs/DEVELOPMENT-STANDARDS.md`](docs/DEVELOPMENT-STANDARDS.md) together when policy changes.*
