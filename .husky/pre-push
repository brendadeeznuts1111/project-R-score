#!/usr/bin/env sh
set -e

# Fast journey proof before the remote learns about a broken install layout.
# Full lint/brands run in CI (harness-gates.yml) and on pre-commit for staged files.
echo "== pre-push: proof:install =="
bun run proof:install
echo "✅ pre-push checks passed"
