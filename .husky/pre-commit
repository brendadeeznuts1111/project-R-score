#!/usr/bin/env sh

# Husky → Bun pre-commit. Pin SSOT: .bun-version + package.json engines.bun / packageManager.
# The runner enforces Bun.semver.satisfies(runtime, engines.bun).
exec bun scripts/pre-commit.ts "$@"
