# HAR CLI

```bash
bun src/cli/har-cli.ts --help
```

Capture / enhance / analyze / diff / lint / serve HAR traffic. Prefer `lib/har-analyzer/` for library use.

Hub: [../../README.md](../../README.md).
