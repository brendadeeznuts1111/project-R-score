#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run tests
echo "Running tests..."
bun test || exit 1

# Run linting
echo "Running Biome check..."
bunx @biomejs/biome check --apply . || exit 1

echo "Pre-commit checks passed!"
