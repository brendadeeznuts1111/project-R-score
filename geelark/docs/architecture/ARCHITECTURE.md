# 🏗️ Geelark Architecture Overview

This document provides a high-level architectural overview of the geelark project structure, component relationships, and data flow.

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                                 GEELARK ARCHITECTURE                                   │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                         │
│  ┌─────────────┐      ┌──────────────┐      ┌─────────────┐                    │
│  │   CLI Layer  │ ───▶ │  Decorators  │ ◀─── │  Middleware │                    │
│  │  dev-hq-cli  │      │  @Route      │      │  Security   │                    │
│  │             │      │  @Middleware │      │  CORS       │                    │
│  └──────┬──────┘      └──────┬───────┘      └──────┬──────┘                    │
│         │                    │                     │                            │
│         ▼                    ▼                     ▼                            │
│  ┌──────────────────────────────────────────────────────────┐                   │
│  │                    Bun Runtime Layer                      │                   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  │                   │
│  │  │ Bun.serve │  │ Bun.file│  │Bun.main │  │Bun.env  │  │                   │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘  │                   │
│  └──────────────────────────────────────────────────────────┘                   │
│                                  │                                                 │
│                                  ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐                   │
│  │                    Core Services Layer                    │                   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐           │                   │
│  │  │ BunServe  │  │BunContext │  │ConfigLoader│           │                   │
│  │  │ HTTP/WS   │  │   Env     │  │  Watcher   │           │                   │
│  │  └───────────┘  └───────────┘  └───────────┘           │                   │
│  └──────────────────────────────────────────────────────────┘                   │
│                                  │                                                 │
│                                  ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐                   │
│  │                      Data Layer                           │                   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  │                   │
│  │  │ Files   │  │  Env    │  │ Network │  │ Process │  │                   │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘  │                   │
│  └──────────────────────────────────────────────────────────┘                   │
│                                                                                         │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

## 📁 Directory Structure

```
geelark/
├── bin/                          # CLI entry points
│   ├── dev-hq-cli.ts             # Main CLI application
│   ├── dev-hq.ts                 # Alternative CLI
│   └── errors.ts                 # Error handling utilities
│
├── src/                          # Core implementation
│   ├── context/                  # Bun runtime context
│   │   └── BunContext.ts         # Bun.main, Bun.env wrappers
│   │
│   ├── config/                   # Configuration management
│   │   └── ConfigLoader.ts       # Bun.file() based config loading
│   │
│   ├── server/                   # HTTP/WebSocket server
│   │   ├── BunServe.ts           # Main server implementation
│   │   └── middleware/
│   │       └── cors.ts           # CORS middleware
│   │
│   ├── security/                 # Security features
│   │   ├── Headers.ts            # Security headers, CSP
│   │   └── TLS.ts                # TLS/HTTPS configuration
│   │
│   ├── decorators/               # TypeScript decorators
│   │   ├── Route.ts              # @Get, @Post, @Put, @Delete
│   │   └── Middleware.ts         # @Middleware decorator
│   │
│   ├── components/               # JSX/TSX components
│   │   └── FormattedOutput.tsx  # CLI UI components
│   │
│   └── preload/                  # Preload scripts
│       ├── test-setup.ts         # Test environment setup
│       └── global-polyfills.ts   # Global extensions
│
├── dev-hq/                       # Dev HQ automation
│   └── core/                     # Automation core
│       └── automation.ts         # DevHQAutomation class
│
├── tests/                        # Test suite
│   ├── unit/                     # Unit tests
│   │   └── type-testing/         # Type testing with expectTypeOf
│   ├── integration/              # Integration tests
│   └── *.test.ts                 # Top-level test files
│
├── docs/                         # Documentation
│   ├── BUN_RUNTIME_FEATURES.md   # Bun features matrix
│   ├── EXPECTTYPEOF_GUIDE.md     # expectTypeOf guide
│   ├── ARCHITECTURE.md           # This file
│   └── ...
│
├── examples/                     # Usage examples
│   ├── BunRuntimeFeatures.ts     # Comprehensive demo
│   └── ...
│
├── bun.toml                      # Bun configuration
├── tsconfig.json                 # TypeScript configuration
├── package.json                  # Dependencies
└── README.md                     # Project overview
```

## 🔄 Data Flow

### CLI Command Execution Flow

```
User Input
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  dev-hq-cli.ts                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 1. Parse CLI arguments (--table, --json, --verbose)    │ │
│  │ 2. Resolve command alias (h → health, i → insights)     │ │
│  │ 3. Auto-correct misspellings                            │ │
│  │ 4. Execute command handler                               │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  Command Handler (insights, health, git, etc.)              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 1. Check system dependencies (Bun, Git, Docker)        │ │
│  │ 2. Run DevHQAutomation for insights                    │ │
│  │ 3. Format output based on flags (--table, --json)      │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  Output Formatting                                           │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ --table → inspect.table()                               │ │
│  │ --json → JSON.stringify()                               │ │
│  │ default → Pretty print                                  │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
Formatted Output to Console
```

### HTTP Request Flow (Bun.serve)

```
HTTP Request
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  BunServe                                                    │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 1. Check WebSocket upgrade                               │ │
│  │ 2. Match route pattern (URLPattern)                     │ │
│  │ 3. Apply middleware chain                                │ │
│  │ 4. Execute route handler                                 │ │
│  │ 5. Apply security headers                                │ │
│  │ 6. Add CORS headers                                      │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  Route Handler (via @Get, @Post decorators)                  │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 1. Parse request body/json                              │ │
│  │ 2. Execute business logic                               │ │
│  │ 3. Return Response                                      │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
HTTP Response with Security Headers
```

### Config Loading Flow

```
Config Request (loadConfig)
   │
   ▼
┌─────────────────────────────────────────────────────────────┐
│  ConfigLoader                                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ 1. Create Bun.file(path) reference                      │ │
│  │ 2. Check file.exists()                                  │ │
│  │ 3. Parse file.json()                                    │ │
│  │ 4. Return typed result                                  │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
   │
   ▼
Typed Config Object
```

## 🧩 Component Relationships

### BunServe + Decorators

```typescript
// Decorators register routes
@Get("/api/users")
getUsers() { return Response.json({ users: [] }); }

// Registration connects decorators to server
registerRoutes(server, APIController);

// Result: HTTP requests route to decorated methods
```

### BunContext + Environment

```typescript
// BunContext wraps Bun.env
const port = BunContext.getEnv("PORT", "3000");

// Provides typed access
const debug = BunContext.getEnvBool("DEBUG");

// Detects entry point
if (BunContext.isMain) { /* ... */ }
```

### Security Headers + Middleware

```typescript
// Middleware runs before handler
@Middleware(middleware.logger)
class API { }

// Security headers applied to response
const secured = createSecurityHeaders()(response);
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│  Security Layers                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1. TLS/HTTPS Layer                                    │   │
│  │    - Certificate loading via Bun.file()             │   │
│  │    - TLS configuration presets                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 2. Security Headers Layer                             │   │
│  │    - CSP (Content Security Policy)                   │   │
│  │    - X-Frame-Options                                  │   │
│  │    - X-Content-Type-Options                           │   │
│  │    - Strict-Transport-Security                       │   │
│  │    - Referrer-Policy                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 3. CORS Layer                                         │   │
│  │    - Origin validation                               │   │
│  │    - Method whitelisting                             │   │
│  │    - Credentials handling                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 4. Middleware Layer                                   │   │
│  │    - Authentication                                 │   │
│  │    - Authorization                                  │   │
│  │    - Rate limiting                                   │   │
│  │    - Input validation                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Design Patterns

### 1. Flag Separation Pattern

```
bun --hot --watch dev-hq-cli.ts insights --table --json
│   │      │      │                │        │
│   Bun      │      Entry Point       CLI Flags
│   Flags     │
├──────────┤
│ Runtime   │  → Handled by Bun
├──────────┤
│ CLI       │  → Handled by dev-hq
```

### 2. Decorator Pattern

```typescript
@Middleware(middleware.logger, middleware.timing)  // Cross-cutting
class APIController {
  @Get("/api/users")                                   // Route definition
  getUsers() { /* ... */ }
}
```

### 3. Context Pattern

```typescript
// Centralized Bun runtime access
BunContext.isMain    // Entry point detection
BunContext.env       // Environment variables
BunContext.version   // Version checking
```

### 4. Builder Pattern

```typescript
const server = new BunServe({ port: 3000 })
  .get("/api/health", handler)
  .post("/api/users", createHandler)
  .websocket({ message, open, close })
  .start();
```

## 📦 Module Dependencies

```
┌─────────────────────────────────────────────────────────────┐
│  Dependency Graph (Simplified)                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  dev-hq-cli.ts                                             │
│      │                                                     │
│      ├──▶ errors.ts                                       │
│      │                                                     │
│      ├──▶ BunContext.ts ◀──── Bun runtime                 │
│      │                                                     │
│      ├──▶ BunServe.ts ◀─────── HTTP server                │
│      │      │                                            │
│      │      ├──▶ Headers.ts (security)                    │
│      │      └──▶ TLS.ts (security)                        │
│      │                                                     │
│      └──▶ Route.ts, Middleware.ts (decorators)            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Extension Points

### Adding New Commands

```typescript
// In dev-hq-cli.ts
program
  .command("my-command", "Description")
  .option("--output", "Output file")
  .action(async (options) => {
    // Command implementation
  });
```

### Adding New Decorators

```typescript
// In decorators/Route.ts
export function Patch(path: string) {
  return Route(path, "PATCH");
}
```

### Adding New Middleware

```typescript
// In middleware/auth.ts
export const auth = async (req: Request, next: () => Promise<Response>) => {
  const token = req.headers.get("Authorization");
  if (!token) return new Response("Unauthorized", { status: 401 });
  return next();
};
```

## 🔗 Related Documentation

- [BUN_RUNTIME_FEATURES](../runtime/BUN_RUNTIME_FEATURES.md)
- [EXPECTTYPEOF_GUIDE](../guides/type-checking/EXPECTTYPEOF_GUIDE.md)
- [CLI_REFERENCE](../api/CLI_REFERENCE.md)
- [RUNTIME_CONTROLS](../runtime/RUNTIME_CONTROLS.md)
