# ============================================================================
# CLAUDE WORKSPACE - ENHANCED IGNORE FOR AGENT/MCP/ACP PERFORMANCE
# Purpose: Optimize code search, reduce indexing overhead, improve agent speed
# ============================================================================

# ----------------------------------------------------------------------------
# BUILD OUTPUT & DISTRIBUTION
# ----------------------------------------------------------------------------
apps/dist/
dist/
out/
.output/
myapp
**/.bun/
**/.bun/install/
*.bun-build
.build-cache/

# ----------------------------------------------------------------------------
# DEPENDENCIES (Critical for search performance)
# ----------------------------------------------------------------------------
node_modules/
**/node_modules/
**/$HOME/
.pnpm-store/
.yarn-cache/

# ----------------------------------------------------------------------------
# ENVIRONMENT & SECRETS
# ----------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.example
.env.production
.env.staging
.env.development
.env.cloudflare
.env.1380

# ----------------------------------------------------------------------------
# TYPE DEFINITIONS & BUILD INFO
# ----------------------------------------------------------------------------
types/
*.tsbuildinfo
tsconfig.tsbuildinfo
**/*.tsbuildinfo

# ----------------------------------------------------------------------------
# DATABASE & CACHE FILES
# ----------------------------------------------------------------------------
*.db-shm
*.db-wal
history.jsonl
.bun-cache/
paste-cache/
shell-snapshots/
file-history/

# ----------------------------------------------------------------------------
# ARCHIVED DIRECTORIES (moved to R2)
# ----------------------------------------------------------------------------
# These are now stored in fw-claude-archive R2 bucket
# Use restore scripts to retrieve if needed
projects/
debug/
bin/
benchmarks/
assets/
examples/
dist/
tests/
scripts/
src/
data/
docs/
apps/
tools/
core/*
cache/

# ----------------------------------------------------------------------------
# V1.3.8 TRACKED FILES (un-ignore from archived directories)
# ----------------------------------------------------------------------------
!docs/BUN_V1.3.8_OFFICIAL_APIS.md
!tests/bun-markdown.test.ts
!src/benchmarks/
!src/benchmarks/bunmark-markdown.ts
!tools/demo/
!tools/demo/markdown-demo.ts
!core/team/
!core/team/**
!.agents/skills/*/scripts/
!.agents/skills/*/scripts/**

# ----------------------------------------------------------------------------
# TELEMETRY & METRICS (NDJSON, large data files)
# ----------------------------------------------------------------------------
telemetry/
traces/
traces/*.md
*.cpuprofile
heap.json
diagnostics.json
validation-metrics.json

# ----------------------------------------------------------------------------
# CDN & EXTERNAL LIBRARIES (Minified, don't lint/search)
# ----------------------------------------------------------------------------
cdn/external/
cdn/staging/
cdn/production/

# ----------------------------------------------------------------------------
# SKILLS (Track .md docs, ignore implementations from search)
# ----------------------------------------------------------------------------
skills/*
!skills/*.md
!skills/**/*.md
skills/**/node_modules/

# ----------------------------------------------------------------------------
# TEST FIXTURES & RESULTS
# ----------------------------------------------------------------------------
# tests/unit/ - No longer ignored, now used for organized tests
test-results/
test-reports/
coverage/
*.lcov

# ----------------------------------------------------------------------------
# DATABASE & RUNTIME DATA
# ----------------------------------------------------------------------------
*.db
*_db
*.db-wal
*.db-shm
data/databases/*.db
data/databases/*.db-*
data/snapshots/
data/screenshots/
data/bench-history.sqlite
feeds.jsonl
history.jsonl

# ----------------------------------------------------------------------------
# LARGE BINARIES & ASSETS (Store in R2, not in search index)
# ----------------------------------------------------------------------------
*.bin
*.pdf
*.zst
bin/app-linux
bin/app-macos
bin/optimized-cdn-test
perf-tool

# ----------------------------------------------------------------------------
# WRANGLER & DEPLOYMENT STATE
# ----------------------------------------------------------------------------
.wrangler/

# ----------------------------------------------------------------------------
# LEGACY & DEPRECATED CODE
# ----------------------------------------------------------------------------
**/legacy/

# ----------------------------------------------------------------------------
# BENCHMARK RESULTS (Timestamped, transient)
# ----------------------------------------------------------------------------
benchmarks/2*/
benchmarks/*.json
benchmarks/*.md
profiles/
CPU.*.md
Heap.*.md
*.cpu.md
*.heap.md

# ----------------------------------------------------------------------------
# OS GENERATED FILES
# ----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ----------------------------------------------------------------------------
# LOGS
# ----------------------------------------------------------------------------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----------------------------------------------------------------------------
# IDE/EDITOR FILES
# ----------------------------------------------------------------------------
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# ----------------------------------------------------------------------------
# TEMPORARY & BACKUP FILES
# ----------------------------------------------------------------------------
*.tmp
*.temp
*.bak
*.backup
*.old
.tmp/
.temp/
temp-*

# ----------------------------------------------------------------------------
# PACKAGE MANAGER LOCKFILES (Use root bun.lock instead)
# ----------------------------------------------------------------------------
package-lock.json
yarn.lock
pnpm-lock.yaml
**/*.lockb

# ----------------------------------------------------------------------------
# DISABLED/ARCHIVED DIRECTORIES
# ----------------------------------------------------------------------------
.git_disabled/

# ============================================================================
# CLAUDE-SPECIFIC TRANSIENTS (High-churn, low-value for agents)
# ============================================================================

# Session transients (very high churn)
debug/
feedback/
file-history/
paste-cache/
shell-snapshots/
todos/
plans/
tasks/
session-env/
statsig/

# Project workspace (2.6GB+ of experimental code)
# Only keep .gitkeep, ignore all project subdirectories
projects/*/
!projects/.gitkeep

# IDE lock files
ide/*.lock

# Network scripts (transient, generated)
scripts/network/

# Cache directories
.bun-cache/
node_modules/.cache/
cache/

# ============================================================================
# HIGH-DUPLICATION DIRECTORIES (From analysis: formatBytes, etc in 6-8 files)
# These are utility-heavy dirs where search results have low signal
# ============================================================================

# Exclude duplicate-heavy generated scripts from search
scripts/matrix/.matrix/
scripts/dashboard/dist/
scripts/core/*.d.ts
scripts/core/*.types.d.ts

# Exclude large app build outputs
apps/**/dist/
apps/**/.output/
apps/**/node_modules/

# Exclude packages build artifacts (source is what matters)
packages/**/dist/
packages/**/node_modules/
packages/**/*.d.ts
!packages/**/src/**/*.ts

# Core WASM build artifacts
core/wasm/*.wasm
core/wasm/target/
core/dist/
core/node_modules/

# ============================================================================
# DOCUMENTATION EXCLUSIONS (For code search focus)
# Track README files, exclude large generated docs
# ============================================================================

# Large markdown files that aren't code documentation
*.md.pdf
APPS_ORGANIZATION.md
IMPLEMENTATION_*.md
MIGRATION_*.md
OPTIMIZATION_*.md
PERFORMANCE_*.md
BENCHMARK_*.md

# Keep essential docs, exclude verbose generated ones
!README.md
!CLAUDE.md
!AGENTS.md
!QUICKREF.md
!SKILL.md

# ============================================================================
# ACP/MCP/AGENT OPTIMIZATION
# Exclude files that slow down context loading and code search
# ============================================================================

# Large JSON data files not needed for code understanding
*.registry.json
*.manifest.json
apps/cdn/staging/*.json
apps/cdn/production/*.json

# Session databases
sessions_db
sessions/

# Temporary analysis outputs (but keep core/analysis/ which is source code)
analysis/
!core/analysis/
reports/
*.report.json
*.analysis.json

# Profiler outputs (already covered above but be explicit)
*.heapprofile
*.heapsnapshot

# ============================================================================
# END OF ENHANCED .claude/.gitignore
# Purpose: Maximize agent/MCP/ACP search performance, minimize index bloat
# ============================================================================
test/results/
